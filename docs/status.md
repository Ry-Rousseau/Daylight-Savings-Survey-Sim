# Status

**Phase:** Phase 6 — Scale to N=100 ✅ **DoD met** (100-agent runs, clean logs, R5/R6/R17). The phase's *real* question — **can a census-seeded silicon sample produce a realistic opinion?** — is **partially resolved**. **Read `docs/checkpoints/phase-6.md` first** (esp. its "central challenge: getting a realistic result" section — the struggle is the pickup point). Phases 0–5 ✅. All P6 work committed to `main` (`14b5f2f`…`d279775`).
**Updated:** 2026-07-22

## Session handoff — the realism struggle (READ THE CHECKPOINT)
The intellectual spine of this session was **getting a realistic result**, summarized here; full narrative in `docs/checkpoints/phase-6.md`.

- **The collapse → the prior → the fix.** The first 100-agent run (Qwen) collapsed to 100% permanent standard time at **tick 0** (before interaction) — the model flattened a genuinely diverse population onto a prior. YouGov calibration revealed *why*: LLMs express the **expert consensus** (standard time), not the **public preference** (DST), wherever the two diverge — robust to model size AND reasoning. **Swapping to Claude Sonnet-5 broke it** (45% DST ≈ real 50%, holds ~83 voice clusters). So the individual-level collapse was a **model artifact**; the residual vote-convergence under interaction is **genuine R10 dynamics**. This is the headline result.
- **Still-open realism problems (the pickup points):**
  1. **Demographic gradient is mis-calibrated (deepest open thread).** Even Sonnet's aggregate is ~right (37–45% DST) but no demographic strongly predicts stance (all Cramér's V <0.2) and the **age gradient is inverted** vs YouGov (ours: young→DST; real: flat/older→DST). Expected lifestyle predictors (chronotype/employment) are absent.
  2. **In-flight, uncommitted:** `sandbox/build_comparison.py` (our-vs-YouGov by age/sex/region; YouGov crosstab transcribed inside) — **run + verify + commit it**; the inverted age gradient is the finding.
  3. **Opinion seeding too weak to steer** — the attractor-flip failed because the X corpus is news-heavy/mislabeled; needs a cleaned pool or **committed** seeding.
  4. **Owed:** a model-swap ADR (Qwen→Sonnet, amends 0005); **adopt Sonnet as the decide/survey baseline** in config/docs.
- **Live vs half-done:** 176 tests green; full figure set + report in `output/figures/` + `phase6_report.qmd`. The comparison figure is the only half-done piece.
- **Known gotchas (see checkpoint):** `build_figures.py` has mojibake dashes (committed PNGs are fine; re-running regenerates garbled subtitles — fix first); per-option calibration is a hand-entered CSV (`phase6_calibration_full.csv`); worktree subagents branch from the last commit (commit before spawning); `data/`+`reference/` gitignored.

## Prior — Phase 6 scoping (historical, superseded by the checkpoint)
Phase 6 is split into two units (one unknown per gate): **P6a** = the persona-realism seeding pipeline (Layer 2); **P6b** = the N=100 scale/infra run consuming P6a's corpus (Layer 1). Decisions taken at P6 start:

- **Population pivots NYC → USA** (ADR 0015, *proposed*). National DST polling exists; NYC has no calibratable figure. Persona wiring built **geography-neutral**; enacting doc updates (`CLAUDE.md`/`PHASE_PLAN.md`/README) held until the ADR is accepted. Validation vs a US DST poll **deferred to P7** (R21).
- **Census sampling is external + already delivered.** A separate pipeline (`notebooks/engineering/acs_pums_national_sample.qmd`, another agent) produced `data/acs_pums_sample_n100.csv` — a nationally-representative ACS PUMS weighted sample, n=100 (cols: sex/age/race/education/occupation/industry/marital/children/region…). P6 builds the *consumer* pipeline, not the sampler.
- **Persona content = real-microdata disposition, NOT demographics-only** (ADR 0016, *proposed* — this **reverses** the earlier demographics-only draft). Why the reversal: demographics-only is a **stereotype amplifier** — given only labels the model reverts to the conditional mode, collapsing within-cell variance and exaggerating between-cell separation. Fix = inject *real* disposition variance from a donor survey via **statistical matching** (NN, model-free), then use the LLM only to *elaborate* fixed real inputs, never invent them.
- **The P6a pipeline (re-runnable, two-stage, cached corpus):** Stage 1 (own engineering notebook) = ACS sample + NN disposition match to a donor survey (**1-NN / distance-weighted, never k-averaging** — averaging recreates the stereotype centroid) → joint dataset. Stage 2 (src-first) per row = **backstory** (LLM, constrained by row+items) → **reflection→anchor** (LLM psychologist/economist lens, synthesizes temperament/risk/values **from the real items, not the backstory** — "Variant A") → assemble. **Hybrid placement:** reflection → always-on `Persona` anchor (R7); backstory + demographic facts + first-person disposition statements → seeded memory @ t=0 (importance@creation, recency@sim-start). Provenance (matched respondent + distance, gen model/prompt/seed) logged (R17/R29). Writes a **versioned `data/personas_corpus_n100.json`**; `Population.from_corpus(...)` loads it → simulations are free/deterministic (seed-time LLM calls happen once, re-runnably).
- **Multi-donor fusion supported, with a caveat:** merging several donor surveys via demographic grouping is fine, but each adds a conditional-independence assumption and cross-donor item pairs carry only demographically-induced correlation, not empirical — pick rich shared keys, don't over-read cross-item correlations. (The reflection step's compression role *grows* as more items are fused.)
- **P6b live run = 3-arm ladder over the same draws:** null (generic ×100, floor) / demographic-only (the *stereotype arm*) / full-pipeline (real disposition). `demographic→full-pipeline` **directly tests the stereotype thesis** (collapsed within-cell variance in demo-only vs realistic spread in full-pipeline). `run_survey` harden pulled forward from P7 (at N=100×3 the un-retried survey path will hit the Qwen3 whitespace quirk).

**P6a Stage-2 pipeline BUILT + green (152 tests, +12 new, 0 failures).** Stage 1 delivered externally: `notebooks/engineering/persona_disposition_seed.qmd` (other agent) → `data/persona_seed_dataset_n100.csv` (100×41: ACS demographics + Gower-1NN donor-matched ANES TIPI/traditionalism/egalitarianism/religion, GSS trust/hours/happiness, ATUS real wake time — each with respondent-id + match-distance provenance, no DST content). Built this session:
- `src/polis/persona_pipeline.py` — `SeededPersona`; model-free `describe_demographics` / `statement_memories` (first-person lived statements) / `reflection_lines` (measures for the LLM); `build_persona`/`build_corpus`; versioned corpus JSON (`corpus_to_dict`/`save_corpus`/`load_corpus`, content hash + provenance, R17).
- **Stage-differentiated models (R6, per user):** `build_persona` takes separate `backstory_client` / `reflection_client` — recommend a **larger model for the reflection** (harder synthesis + clean JSON), baseline/creative for backstory; stage-appropriate temps (backstory 0.9 warm, reflection 0.3 cool); each stage's model+temp logged in provenance. Seed-time is one-shot + cached, so a big reflection model adds no per-run cost.
- **Party/ideology excluded from persona content by default** (ADR 0016 judgment call — DST is non-partisan; avoid re-stereotyping); available for opt-in ablation.
- **Cross-donor guard:** work-hours statement suppressed when ACS says not-working (the multi-donor CI caveat, caught live — a retiree was "working 40h").
- `src/polis/prompts.py` — `backstory_user` + `reflection_user` (Variant A: reflection reads the real items, not the backstory).
- `Persona.location` + `persona_system(location=...)` — backward-compatible (default NYC → P5 null-baseline byte-identity preserved); census personas carry real US state (ADR 0015).
- `Population.from_corpus(path|dict|list, *, client, embedder)` — loads the cached corpus, seeds each agent's t=0 memories, stashes `corpus_meta` (hash) cited in the run config (R17). No seed-time LLM at run time.
- `run_survey` skip observability (retry/skip already merged in `phase-5-hardening`): skips now logged + `return_skipped` returns `(answers, skipped_ids)`, threaded through `Population.survey`.
- Verified live over the real 100 rows with a stub client: 100 personas, deterministic hash, all donors + memories intact, 0 crashes. **On `main`, uncommitted (commits are the user's).**

**P6a corpus GENERATED (live, one-shot, cached).** `notebooks/engineering/persona_pipeline.qmd` (+ `sandbox/run_persona_corpus.py` runner) generated **`data/personas_corpus_n100.json`** — 100/100 personas, **0 failures, 0 empty anchors**, ~11 min, content_hash `f3a1042b…`. Models: **backstory `qwen/qwen3-235b-a22b` (0.9) + reflection `qwen/qwen3.7-max` (0.3)**, both `reasoning=False` (Gemini 3.1-pro was tried but has *mandatory* reasoning → truncated JSON; Qwen-max is simpler + same-family). Each persona: factual description, 2 values + 1–2 dispositions (R7 anchor from the reflection), 3 t=0 seed memories (demographic facts / one combined disposition-statements memory / backstory), full R29 provenance (donor ids+distances, per-stage model/temp, `data_flags`).

**Persona-content refinements (this session, from user review):** reflection prompt is **measures-only** (demographics stripped — withholding beats instruct-to-ignore for stereotype); explicit **group-facilitator lens** (Park-style); backstory decluttered (dropped DST/opinion-avoidance that only primed); disposition statements kept as **one memory** (user's call). **Data hygiene (scan of all 100):** the only cross-donor fusion artifact is **not-working ACS + donor GSS work-hours (32/100)** → `sanitize_row` bakes it out (nulls the donated hours before any stage sees it); the **9/100 ANES religion tensions are within-donor** ("spiritual but not religious" — a real profile) → **flagged-but-preserved**, reflection reconciles. `no-own-children + family_size>1` (26) is *not* a contradiction. All logged in `provenance.data_flags = {resolved, flagged}`. **156 tests green.**

**Next — P6b (needs spend approval):** the 3-arm N=100 run (null / demographic-only / full-corpus) on `small_world`, consuming this corpus via `Population.from_corpus`. `run_survey` skip-observability already done. Cite the corpus content_hash in the run config (R17, wired).

## Prior — Phase 5A (persona depth, R7–R9)
Phase 5 is two axes at once (Persona + Validation), split into three units: **5A persona depth** (done), **5B validation dashboard** (embedding pairwise-distance / cluster count / continuous dashboard / R16 null baseline / R27 adequacy), **5C live DoD run + checkpoint** (thin vs thick × drift trajectory × dashboard vs null). 5A first because the R16 null baseline is only meaningful once personas are the controlled variable (ADR 0012).

**5A built and green (112 tests, +23 new, 0 failures).** `Persona` now carries value/disposition anchors (R7), composed into the system prompt; the **empty persona is byte-identical to P0–P4** (pinned by a regression test) so the thin persona survives unchanged as the **R16 null-model baseline**. New `personas_nyc.py`: a hand-authored thick NYC cast (morning-bright cluster / evening-late cluster / one ambivalent / one low-conviction drift subject) + `NULL_PERSONA` + `null_cast(n)`. New `drift.py`: the **R8 persona-strength probe** — embeds the free-text *reason* (voice, where collapse shows), reports `drift_from_baseline` (R8) and `distance_to_centroid` (R9) side by side, re-asked with `remember=False` so measuring doesn't perturb the memory it measures. Persona anchors versioned in the run config (R17). **Two project decisions (ADR 0013):** (1) no LLM self-hosting ever — stay on OpenRouter, amends ADR 0002; (2) the conviction-slider end-goal (seed-time X-corpus injection, sibling of the ADR-0010 runtime feed) is left as a seam on the new anchors, not built. Brief: `docs/briefs/phase-5a-persona-depth.md`.

## Prior — Phase 4 (Topology, merged to `main`)
Phase 4 built and green on branch **`phase-4-topology`** (merged to `main` at `11e3024`). Topology is now a **pluggable, seeded, swappable** parameter (R4/R10/R13), and swapping it — same personas, different graph — produces a **starkly different homogeneity metric**, the Phase-4 spike. **Premise challenged first (ADR 0012):** the DoD needs a "measurably different homogeneity metric," but the full metric layer (R14–R17 dashboard + R16 null baseline) is a P5 deliverable and R16 only means something once personas are the controlled variable — so P4 builds the **minimal tested kernel** (`metrics.homogeneity`, defined+tested before the run per R14) that P5 extends, not the full dashboard. Headline signal = the free per-tick logged-stance trajectory + one endpoint survey per config. You chose all of R4+R10–R13 this phase and a live run.

New: `topology.py` (`Topology` base + `FullyConnected`/`RingLattice`/`SmallWorld`/`StochasticBlock`, seeded, symmetric, frozen adjacency, `to_config()` for R17; drop-in for the callable seam); `metrics.py` (`homogeneity`/`stance_distribution`/`homogeneity_trajectory`, reads the `action` stream offline). Changed: `simulation.py` (`DynamicsConfig.exchange_volume` R12 + deterministic reach sampling in `_resolve`; config versions the structured topology descriptor + exchange volume + committed roster R17); `agent.py` (`committed_stance` + `committed()` helper — immovable faction, **no model call**, R11); `runlog.py` (`EVENT_TIE_CHANGE` reserved as the R26 seam, not emitted). **89 deterministic tests pass** (53 P0–P3 unchanged + 36 new: 14 topology, 11 metrics, 11 simulation).

**Live DoD met:** `notebooks/experiments/phase4_topology.ipynb` ran (`qwen/qwen3-32b`, 20 agents [10 evening / 10 morning] × 4 ticks, **0 failures**). Same personas, three topologies: `fully_connected` **collapses to unanimity by tick 1** (dominant share 0.50→1.00, entropy→0.00) while `small_world` and `stochastic_block` **hold the split** all run (0.55 / entropy ≈ 0.50) — R10 (full exposure → global consensus) confirmed; endpoint surveys agree. Committed minority (R11), same 4-agent faction: **0%** non-committed adoption under full connectivity vs **31%** under clustering — full exposure *swamps* the minority via the same consensus cascade, clustering *shelters* it (R10+R11 in one comparison). Results `data/phase4_topology.csv`. Checkpoint `docs/checkpoints/phase-4.md`; ADRs `0011`/`0012`.

**Phase 3** (concurrent scheduler R5 + latency/token/cost logging R6; ADR 0009): merged to `main`. `notebooks/experiments/phase3_throughput.ipynb`, `data/phase3_throughput.csv`; checkpoint `docs/checkpoints/phase-3.md`. Throughput scaled 5.7× (0.24→1.36 decides/s) at 25 agents; endpoint concurrency-bound. Sequential scheme gains no throughput (serial by R28). `est_cost_usd` null unless `PRICE_PER_MTOK` set.

**Phase 2** (Game Master / interaction; ADRs 0006/0007/0008): merged to `main`. SQLite run log tier-3 (R17), world tier-2 (R2/R3), SPEAK/ABSTAIN (R23/R25), custom tick loop (R22/R28); checkpoint `docs/checkpoints/phase-2.md`.

**Phase 1** remains complete on `phase-1-memory` (merged to `main`): per-agent memory, retrieve → inject → **unchanged** `choose()` → R19 writeback; DoD P(permanent-DST) delta +1.000. Checkpoint `docs/checkpoints/phase-1.md`; ADRs `0004`/`0005`. torch cu124 `cuda True` on the RTX 4070 Ti SUPER.

## Model-capability addendum — done (ADR 0005)
Swept the two-agent DoD across same-family **Qwen3 8b → 14b → 32b** (no Qwen3-72B exists), K=15, annotated + plain wording. Memory delta = 1.0 at all sizes (annotated); the option annotation is an **8B-only crutch** (plain-wording delta: 8b 0.0, 14b/32b 1.0); larger model *less* collapsed at baseline (weak single-persona proxy — real convergence test is P4/P5). **Baseline revised to `qwen/qwen3-32b`** (`llm.py:DEFAULT_MODEL`; amends ADR 0002; P5 self-host → Qwen3-32B-AWQ). Evidence: `notebooks/experiments/phase1_model_sweep.ipynb` + `data/phase1_model_sweep.csv`. Drop-back to 14B if P6 cost/latency bites.

## 5B built and green (131 tests, +19 new, 0 failures)
`metrics.py` extended into the full R14 layer (ADR 0014): **`pairwise_dispersion`** (mean pairwise cosine distance) + **`cluster_count`** (single-linkage components at an explicit, tunable cosine threshold, default 0.15) over the SPEAK **utterance** embeddings — the continuous complement to the categorical stance read, catching within-stance wording homogenization the tally hides. **`divergence_trajectory`** = the R15 continuous per-tick dashboard merging *both* axes (dominant-share/entropy + dispersion/cluster-count), embedding each distinct utterance once; **`divergence_summary`** = the endpoint bundle a notebook computes for **null vs thick** runs (R16). **`action_space_adequacy`** = the **R27** gate as a *separate* diagnostic (abstain rate, stance coverage, utterance uniqueness/dispersion + `all_abstain`/`single_stance`/`low_utterance_variety` flags) — read *before* trusting any convergence number. `stance_distribution` refactored onto a shared `latest_speaks` reader (P4 tests pass unchanged). `metrics` stays a submodule import (consistent with P4). Brief: `docs/briefs/phase-5b-validation-dashboard.md`.

## 5C done — the P5 result (live, 0 failures)
Live run (12 agents × 5 ticks, `small_world`, `qwen/qwen3-32b`): **thick NYC cast vs the thin `NULL_PERSONA`** baseline (R16), identical memories/topology/seed/temperature — persona content the only difference. **Both arms converge on stance** (dominant-share → 1.00, permanent standard time) — the categorical read calls both "converged." **The embedding axis (5B) splits them:** thick holds **8 voice-clusters / dispersion 0.20 / drift 0.127**; null **collapses to 1 cluster / 0.12 / drift 0.191**. So **persona depth protects the *voice*, not the *vote*** — R7 anchoring resists voice collapse; R9 confirmed (stance consensus still emerges despite per-agent anchors). R27 gate passed (abstain 0.00, no flags → real convergence, not artifact). Notebook `notebooks/experiments/phase5_persona_validation.ipynb` (executed, plots+verdict inline); results `data/phase5_persona_validation.csv` + `data/phase5_drift.csv`; checkpoint `docs/checkpoints/phase-5.md`.

## Phase 6 plan — see the "Now" section above
Scope, decisions, and the 3-arm run design are in the **Now — Phase 6** section above and in `docs/briefs/phase-6-scale.md`. Key change from the earlier plan: the P5 *thick-vs-null* contrast becomes a **3-arm ladder** (null / demographic / thick) over identical census draws, with **demographics-only as the estimator** (not the P5 hand-authored NYC cast, which is now NYC-flavoured legacy after the USA pivot). Census sampling is delivered externally as a DataFrame; P6 builds the consumer. Voice-cluster threshold swept at N=100. `run_survey` harden pulled forward from P7.

## Open threads
- **External-signal feed (ADR 0010) — seam added, off by default.** `src/polis/feed.py`: `FeedProvider` seam (`NullFeedProvider` default → zero behaviour change), `ScriptedFeedProvider` (deterministic/from-file shape), `RagFeedProvider` **stub** (retrieval over the X corpus — deferred, ToS-sensitive + needs the P5 metric to interpret). Delivered at tick start into the target agent's memory as `KIND_FEED`, logged as `EVENT_FEED` (R3), `feed_provider` in run config (R17). 6 tests. This is the runtime-events path; the sibling *seed-time opinionated-injection* ("slider" from the brief) is not built. Wire real X data by loading curated posts into a `ScriptedFeedProvider` schedule, or implement `RagFeedProvider`.
- **Phase 8 — Interactive GUI (optional/stretch) added to the plan.** Thin sliders-and-run front-end over the existing config fields + a live divergence plot; UI calls `src/polis`, adds no engine logic; slots in after P5 (needs the divergence metric to display). Not started — a plan entry only.
- **Run-level architecture — RESOLVED at P2.** Durable run log (SQLite, ADR 0006), `Simulation`/`Population` container + custom tick loop, world-state store (R2/R3), and R29 retrieval provenance are all built and green. Standing call held: retrieval store stays in-RAM numpy; persistence is additive, no global vector index (R2).
- **R27 action-space adequacy — RESOLVED at P5.** `metrics.action_space_adequacy` (ADR 0014) is the separate diagnostic (abstain rate / stance coverage / utterance uniqueness + degeneracy flags), read *first* in the 5C notebook before the divergence numbers. The 5C run passed clean (abstain 0.00, no flags), so the convergence result is genuine, not an action-space artifact.
- **Survey-layer resilience — DONE (was stale).** The P5 checkpoint's debt note ("`run_survey` has no retry/skip, one bad agent aborts the survey") was **superseded** by the `phase-5-hardening` branch (commit `9bc2985`, merged to `main` at `237c661`) — written *after* the P5 checkpoint. `graph._ask` now wraps `agent.answer` in `retry_on_llm_error` (catches `LLMError`, 3 tries, `None`→skip); the Qwen3 whitespace blob becomes an `LLMError` inside `choose` (JSON parse fail → retries exhausted). So per-agent retry+skip already exists. **One real gap left for N=100:** skips are **silent** — `_ask` returns `{"answers": []}` with no count/log, so a 100-agent survey that loses k agents looks like a clean (100−k) result. Remaining harden = surface a skipped count + ids (P6b, small).
- **Topology built (P4), R26 tie-mutation still a seam.** `src/polis/topology.py` gives 4 pluggable seeded graphs (R4/R10/R13). Topology-*mutating* actions (R26) are reserved only (`EVENT_TIE_CHANGE` in `runlog.py`, not emitted) — the static graph satisfies R13's counterfactual; endogenous tie formation/dissolution is a later dynamics feature with its own resolution logic. **Committed-minority direction is counter-intuitive** (full connectivity *reduces* a minority's reach by swamping it via the consensus cascade) — see checkpoint §4 so P5 doesn't mis-read it.
- **World tally still not read by agents:** `WorldState.stance_tally` is logged (R3) but agents don't condition on it — P4's dynamics came from *heard memory over the topology*, not the tally. Turning the tally into a live consensus pressure remains an unused P5+ lever.
- **Push:** `origin` set (`github.com/Ry-Rousseau/NYC-Daylight-Savings-Sim`); `main` up to date with **Phases 0–3 merged**. `phase-4-topology` branch is local/uncommitted, awaiting review (commits are the user's).
- **Endpoint:** **OpenRouter** `qwen/qwen3-32b` for the whole project — **self-hosting dropped** (ADR 0013 amends 0002/0009; the `executor="batch"`/`Qwen3-32B-AWQ` self-host leg is gone). Details: `docs/query_handbook.md`. Local 4070Ti reserved for lightweight local models (e.g. embeddings).
- **Census dataset (P6):** sampling is **external** — a separate pipeline delivers a DataFrame of N rows drawn from a larger census dataset; P6 builds only the consumer `Population.from_census(df)` against the schema contract in the brief. Frame is **US** (ACS), not NYC PUMS, after the ADR-0015 pivot. `data/nyc_puma_crosswalk_2020.csv` retired from load-bearing → optional enrichment.
- **Validation ground truth:** a published **US** DST opinion figure (national polling exists; NYC's did not — the reason for the ADR-0015 pivot). **Deferred to P7** (R21 calibration hold-out); P6 produces the estimate, P7 scores it.
- **viz_theme:** categoricals deferred until the persona schema firms up (~Phase 5).

## Resolved this session (Phase 4)
- **Topology layer (ADR 0011):** a `Topology` class hierarchy that *is* the existing callable seam (drop-in, no tick-loop change) over plain closures or a networkx dependency — each graph exposes `to_config()` for R17 and builds a frozen, seeded, symmetric adjacency once. Ships `FullyConnected`/`RingLattice`/`SmallWorld`/`StochasticBlock` + `exchange_volume` (R12) + committed-minority affordance (R11). **Static topology only:** R26 tie-*mutating* actions held out (one unknown per phase); static swap satisfies R13's counterfactual, seam reserved (`EVENT_TIE_CHANGE`).
- **P4/P5 metric boundary (ADR 0012):** the DoD needs a metric but R14–R17 is P5 and the R16 null baseline is premature against thin personas. Resolved by building the **minimal tested kernel** (`metrics.homogeneity`, defined+tested before the run — satisfies R14 literally without front-running P5); P5 extends the same kernel. Headline signal chosen = free per-tick logged-stance trajectory (shows *when* convergence happens, R15 rationale) + one endpoint survey/config as confirmation.
- **DoD result is decisive:** full connectivity → unanimity by tick 1 (1.00 / entropy 0.00); sparse/clustered → split preserved (0.55 / 0.50). Committed-minority finding *inverted* the naïve hypothesis (0% vs 31% adoption) — full exposure swamps a minority, clustering shelters it. Notebook §4/verdict markdown corrected post-run to match the data.
- **Prior sessions' setup (env, `.venv`+`polis` kernel, `pyproject.toml`, rule/doc reconciliation, Phase 0) remain as recorded in the checkpoints and earlier handoffs** — Phases 0–3 are merged to `main`; not re-litigated here.

## Handoff format
On a state-changing session end: update this file — what changed, what's live vs half-done, decisions + why, blockers. Any decision gets an ADR in `docs/adr/` (format: `docs/adr/template.md`).

# ADR (Architecture Design Record) NNNN: <short title>

Status: proposed | accepted | superseded by ADR NNNN
Phase: <phase number from the phase planning doc>

## Spike question

<the specific question from phase doc this ADR resolves>

## Context

<what needed deciding, and why — constraints in play, e.g. hardware limits,
a specific rule from ARCHITECTURE.md that bears on the decision>

## Options considered

1. <option> — <tradeoffs>
2. <option> — <tradeoffs>

## Decision

<what was chosen>

## Why

<the deciding factor(s) — be specific enough that future-you, mid-debug in
a later phase, can tell whether this decision is a plausible root cause>

## Consequences

<what this locks in, what it forecloses, what to revisit if assumptions
change (e.g. "revisit if agent count exceeds X" or "revisit if switching
base model")>

## Rules touched

<which R-numbers from ARCHITECTURE.md this decision implements or
constrains>